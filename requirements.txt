# Copyright 2026 Anthropic PBC
# SPDX-License-Identifier: Apache-2.0
#
# The eight packages of this repository, editable from their directories, and every
# third-party package they need at run time, pinned to the versions the release was
# tested against. Install with `pip install -r requirements.txt` inside a virtualenv;
# `scripts/install.sh` runs that line. Nothing here resolves a repository package from
# the package index: the eight names are unregistered there, and each pyproject pins its
# sibling dependencies to a version no index carries. To move a pin, edit it here and in
# the pyproject that declares the range, then run the verify line in the README.

-e ./commerce-model-runtime[anthropic]
-e ./commerce-common[examples]
-e ./shopping-agent/core
-e ./shopping-agent/runtime-messages-api
-e ./shopping-agent/runtime-agent-sdk
-e ./merchant-agent/core
-e ./merchant-agent/runtime-messages-api
-e ./merchant-agent/runtime-agent-sdk

annotated-doc==0.0.5
annotated-types==0.8.0
anthropic==0.122.0
anyio==4.14.2
attrs==26.1.0
certifi==2026.7.22
cffi==2.1.1
claude-agent-sdk==0.2.139
click==8.4.2
cryptography==50.0.0
distro==1.9.0
fastapi==0.141.1
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
httpx-sse==0.4.3
idna==3.18
jiter==0.16.0
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
mcp==1.26.0
pydantic==2.12.5
pydantic-core==2.41.5
python-dotenv==1.2.1
pyyaml==6.0.3
referencing==0.37.0
rpds-py==0.30.0
sniffio==1.3.1
starlette==0.52.1
typing-extensions==4.15.0
typing-inspection==0.4.2
uvicorn==0.40.0
websockets==16.0
